[project]
name = "memory-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.28.1,<1.0.0",
  "mcp>=1.27.1,<2.0.0",
]

[project.scripts]
memory-mcp = "memory_mcp.server:main"
memory-mcp-bench = "memory_mcp.bench:main"
